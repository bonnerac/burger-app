{{#each burgers}}
{{#unless devoured}}
<ul>
    <li>
        <p>Burger: {{this.burger_name}}</p>
    </li>

</ul>
<button type="submit">Devour it!</button>
{{/unless}}

{{/each}}

<form action="/" method="POST">
    <textarea type="text" name="burger"></textarea>

    <button type="submit">Create New Burger!</button>
</form>

{{#each burgers}}
{{#if devoured}}
<ul>
    <li>
        <p>Burger: {{this.burger_name}}</p>
    </li>

</ul>
{{/if}}
{{/each}}